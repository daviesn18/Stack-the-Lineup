import { SetPasswordPage } from '@/workbench/AuthPages';

/** Shown after an invite or password-reset link, before anything else. */
export default function SetPassword() {
  return <SetPasswordPage />;
}
